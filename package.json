{
  "name": "ai-gateway-routing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Task-type-based model routing on the Vercel AI Gateway.",
  "scripts": {
    "demo": "tsx examples/demo.ts",
    "e2e": "tsx examples/live-e2e.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "eval": "tsx eval/run-eval.ts",
    "check": "npm run typecheck && npm run test && npm run eval"
  },
  "dependencies": {
    "@ai-sdk/gateway": "^1.0.0",
    "@openrouter/ai-sdk-provider": "^1.5.4",
    "ai": "^5.0.0",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "@vercel/node": "^13.0.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^5.0.1"
  },
  "overrides": {
    "undici": "^7.16.0"
  }
}
